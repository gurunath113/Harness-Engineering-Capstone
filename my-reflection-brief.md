# Reflection Brief — Harness Engineering Capstone

**Name:Kummara Gurunath**
**Date:24/09/2026**

Replace each `→` with your answer. **Every answer cites at least one artifact from your own runs** — a run ID, file path, token count, claim outcome, or test count. Uncited answers do not pass. 3–6 sentences each unless noted. Paste short artifact snippets where they help.

**Environment**

- Model(s):
- OS / Python:
- Approx. API spend:

---

## Part 1 — Per-system

### System 1 — Agentic loop

1. **Loop control.** Quote the `stop_reason` sequence from one trace. Name the file and function that decides continue-vs-stop, and how.
   → The agentic loop is controlled by the Claude response stop_reason, rather than by searching the assistant's text or relying on a fixed iteration count. For the claim_01_kitchen_fire trace, the sequence was tool_use followed by end_turn, giving two turns before the loop stopped; the run summary records 2 turns for that claim. The implementation in claims_intake/loop.py continues when stop_reason == "tool_use" and returns when it is "end_turn", with unexpected reasons treated separately. The overall System 1 test suite also passed all 29 tests.

2. **Anti-pattern.** Name one anti-pattern `test_antipatterns.py` checks for. What would break in your run if the loop used it?
   → One anti-pattern checked by test_antipatterns.py is using assistant-text string membership to decide control flow. That would make the harness dependent on wording generated by the model instead of the API's structured response metadata, so a harmless wording change could alter execution. The System 1 test evidence shows the loop and tool tests completed successfully with 29 passed, supporting that the implementation avoided those prohibited control-flow patterns.

3. **Tool design.** Pick two tools with overlapping inputs. How do the descriptions prevent misrouting? What did a structured tool error let the agent do that a generic string would not?
   → The System 1 tool layer separates responsibilities instead of making the model choose between overlapping generic operations. Tool descriptions and structured results provide explicit information for the model to select the appropriate operation, while tool failures are represented structurally rather than being hidden inside arbitrary prose. This matters because the harness can reason about a failed tool call as data instead of trying to infer failure from a natural-language error message; the System 1 tool tests are included in the successful 29-test run.

4. **Your numbers.** Quote the turn count and cost for one claim. How does it differ from the README sample, and why?
   → TFor claim_01_kitchen_fire, the run used 2 turns, with 6,387 input tokens, 486 output tokens, and an estimated cost of $0.0088. The complete run processed 8 fixtures for an estimated total of $0.0952. This differs from the README/sample-style expectation because the actual recorded run depends on the real model responses and the specific fixtures processed; therefore, I used the measured run artifact rather than treating the sample numbers as authoritative.

### System 2 — Context strategy

5. **The reduction.** From `budget.json`: baseline tokens, assembled tokens, reduction %. Which section dominates the assembled context, and why keep it verbatim?
   → The baseline context measured 38,708 tokens, while the assembled context measured 16,804 tokens, producing a 56.59% reduction. The dominant section was the active conversation at 15,789 tokens, compared with only 204 tokens for case facts, 357 for resolved refund information, and 472 for the resolved subscription section. This shows why the assembly strategy focuses compression on resolved history while preserving the active working context.

6. **Summarize vs preserve.** State the rule for what gets summarized vs kept byte-exact, citing your per-section token numbers.
   → The strategy preserves small, high-value factual sections directly while compressing resolved conversational history. The measured sections were case_facts: 204 tokens, resolved_refund: 357, resolved_subscription: 472, and active: 15,789. The compression API itself processed large inputs—12,334 tokens for refund history and 11,475 tokens for subscription history—before producing much smaller outputs of 344 and 459 tokens respectively..

7. **Facts block.** Compare `eval.jsonl` to `eval_control.jsonl`. Which question regressed, and what does that prove?
   → The evaluation demonstrates that the assembled context preserved the structured status in_progress for the active payment-method issue. In the control result, the model instead claimed that no exact structured status token existed, even though the expected fragment was in_progress; the engineered-context evaluation returned the exact token and passed. This is evidence that the context strategy was not simply about reducing tokens—it also preserved information in a form that remained retrievable.

### System 3 — Claude Code config

8. **Path-scoped rules.** Quote the glob frontmatter from one rule file. Why is it better than a directory-level CLAUDE.md for cross-cutting conventions?
   → The evidence contains path-scoped rule artifacts including evidence_rule_react.md, evidence_rule_api.md, and evidence_rule_tests.md. [NEEDS the contents of one rule file to quote the exact glob frontmatter.] Path-scoped rules are useful because they apply conventions only to the files matching the relevant path pattern, rather than imposing the same rule on the entire repository. This makes cross-cutting conventions precise while avoiding unrelated directories inheriting rules that do not apply to them.

9. **Forked skill.** Quote the `context: fork` and `allowed-tools` lines. What does running forked + read-only buy you? What breaks without it?
   → The forked skill is represented by evidence_rule_skill.md, and the evidence collection completed with OK from the validator. [NEEDS the contents of evidence_rule_skill.md to quote the exact context: fork and allowed-tools lines.] Running the skill in a forked context with a read-only tool allowlist isolates its work and prevents it from making unintended repository modifications. Without those restrictions, the skill could have a larger execution context and potentially perform operations outside the intended read-only scope.

10. **Scope.** From the validator output: project-level vs user-level scope. Give one example of each from this config.
    → The validator artifact is evidence/system3_claude_config/validator_output.txt, whose recorded result is OK. The configuration evidence also includes claude_dir_structure.txt and the saved Claude rule artifacts. [NEEDS validator/config contents to identify the exact project-level and user-level examples.] The answer should name one concrete project-level configuration and one concrete user-level configuration from the validator rather than assuming their scope.

### System 4 — Orchestration

11. **Push work down.** Defects the SQL query returned vs warm-tier total. Name the indexed query. Why does the model never see the full history?
    → The System 4 shift-run artifact is evidence/system4_orchestration/shift_run_output.txt. The captured run reports 0 new defects for shift C, while the scratchpad records the resulting conclusion. [NEEDS the relevant SQL/query artifact to give the exact defects-vs-warm-tier total and indexed query.] The architectural principle is that the database performs the filtering/aggregation first, so the model receives only the relevant result rather than the entire historical dataset. This reduces context size and keeps raw history outside the model's context.

12. **Crash recovery.** The resume-vs-fresh decision and its staleness threshold (`recovery.py`). Why is a fresh start with an injected summary sometimes more reliable than resuming?
    →The recovery implementation defines STALE_RESUME_THRESHOLD_MINUTES = 30. Empty state or already-complete state results in fresh; an incomplete state whose latest step is within 30 minutes results in resume; an older incomplete state results in fresh. The rationale in the implementation says the 30-minute window is approximately 1/16 of an 8-hour shift, while stale partial work should restart using the findings already captured in the manifest as a summary.

13. **Small state.** Byte size of your `hot_state.json`. Why does the budget matter for a system run once per shift, indefinitely?
    → The captured hot_state.json is 643 bytes, as shown by the System 4 verification output: -rw-r--r-- ... 643 ... data/hot_state.json. The corresponding evidence file is evidence/system4_orchestration/hot_state_size.txt. Keeping state small matters because this system can run indefinitely, once per shift, so unnecessary growth would eventually increase storage, recovery, and context costs. A small bounded state also makes recovery predictable rather than allowing historical state to grow without limit.

---

## Part 2 — Synthesis

*Graded on connecting two or more systems. Cite a named file/artifact from each.*

14. **Three layers.** Point to a file/artifact for each layer and justify.
→ Model: evidence/system1_agentic_loop/claim_01_kitchen_fire.jsonl — demonstrates the model's structured stop_reason and tool calls.
→ Harness: claims_intake/loop.py and evidence/system1_agentic_loop/pytest_S1.log — the harness interprets stop_reason and the test artifact records 29 passed.
→ Orchestration: evidence/system4_orchestration/shift_run_output.txt and hot_state_size.txt — the orchestration layer performs the shift run and maintains bounded state, with the captured hot state measuring 643 bytes.

15. **Deterministic vs prompt.** Cite one behavior guaranteed in code (terminal tool, read-only allowlist, atomic write, byte budget) and one guided by prompt. When is each right?
    → A deterministic behavior is the read-only/tool enforcement and loop control in code, such as claims_intake/loop.py checking structured stop_reason values rather than relying on natural-language instructions. Another deterministic example is the System 4 bounded state measurement, where hot_state.json is verified at 643 bytes. Prompt-guided behavior is appropriate for decisions involving interpretation or communication, while deterministic code is preferable for safety boundaries, termination conditions, permissions, and resource limits that must hold regardless of model wording.

16. **Context, two faces.** Compare context management in System 2 (intra-session) and System 4 (cross-session) with cited numbers from both. Same principle, different mechanism — how?
    → System 2 manages context within a session by reducing and assembling information according to a token budget, with the exact baseline and assembled counts recorded in budget.json. System 4 manages context across sessions using persistent state, with hot_state.json measured at 643 bytes and a scratchpad artifact of 322 bytes. Both use the same principle: keep the information necessary for the next model action while preventing irrelevant historical information from accumulating. System 2 achieves this through context assembly/reduction, while System 4 achieves it through bounded persistent state and recovery mechanisms.

17. **Reliability you can't see in one run.** Name one behavior a test guarantees that a single successful run would not reveal. Why does it matter before shipping?
    → System 1's tests/test_antipatterns.py guarantees that the implementation does not fall back to string-membership checks or integer-literal iteration caps as its primary stopping mechanism. A single successful trace could show the correct tool_use → end_turn sequence without revealing that the implementation might fail under a different response or code change. The pytest artifact records 29 passed, providing evidence that these structural properties were checked before shipping. This matters because reliability includes behavior under failure and edge cases, not just one successful execution.

18. **Blast radius.** Pick one system. What's the blast radius if it misbehaves, and what's the kill switch? Ground it in that system's tools, enforcement points, and state.
    → System 1 has a relatively contained blast radius because its tools and loop control are explicitly enforced by the harness. The loop only continues for the structured tool_use stop reason, ends on end_turn, and raises for unexpected values; the anti-pattern tests additionally prevent unsafe stopping logic. The practical kill switch is therefore the harness/test boundary and the explicit unexpected-stop exception, rather than allowing an uncontrolled loop to continue indefinitely. The trace claim_01_kitchen_fire.jsonl demonstrates the intended tool_use → end_turn lifecycle.

---

## Part 3 — Honest assessment

19. **What broke.** One thing that failed first try in your environment, and how you fixed it. (If nothing, what you checked to be sure.)
    → One practical issue during evidence packaging was that the environment did not provide the expected zip/unzip shell commands, so the initial packaging approach could not be used directly. I worked around that by using Python's built-in zipfile module to create and inspect Harness_Engineering_Evidence.zip. The final evidence structure was then verified rather than assuming that the archive had been created correctly.

20. **What you'd change.** One architectural decision you'd make differently, grounded in what you observed.
    → The biggest architectural change from this project is that I would no longer treat the prompt as the primary reliability mechanism. The completed systems show the value of moving control decisions into deterministic code, keeping context deliberately assembled, separating hot/warm/cold state, and using orchestration for persistence and recovery. The evidence from System 2's 56.59% context reduction and System 4's bounded 643-byte hot state made the cost and reliability benefits of that architecture concrete.